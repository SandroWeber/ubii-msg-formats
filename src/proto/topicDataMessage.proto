package topicData;
syntax = "proto3";

message topicDataMessage {
    string message_type = 1; 
    string device_identifier = 2;
    repeated publishTopicData publish_topic_data = 3;
}

message publishTopicData {
    string topic = 1; 
    string data = 2; 
}