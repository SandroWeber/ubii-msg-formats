package ubii.service.request;
syntax = "proto3";

message DeviceRegistration {
    string name = 1;
    enum DeviceType {
        PARTICIPANT = 1;
        WATCHER = 2;
    }
    DeviceType device_type = 2 [default = PARTICIPANT];
    string corresponding_client_identifier = 3;
}