syntax = "proto3";
package ubii.service.request;

message DeviceRegistration {
    string name = 0;
    enum DeviceType {
        PARTICIPANT = 0;
        WATCHER = 1;
    }
    DeviceType device_type = 1 [default = PARTICIPANT];
    string corresponding_client_identifier = 2;
}
