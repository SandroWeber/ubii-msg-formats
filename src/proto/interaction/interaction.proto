syntax = "proto3";
package ubii.interaction;

import "./ioMapping.proto";

message Interaction {
    string id = 1;
    string name = 2;
    string callback = 3;
    repeated IOMapping inputs = 4;
    repeated IOMapping outputs = 5;
}