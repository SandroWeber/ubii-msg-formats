package ubii.service;
syntax = "proto3";

import "./request/clientRegistration.proto";
import "./request/deviceRegistration.proto";
import "./request/subscribtion.proto";

message ServiceRequest {
    oneof type{
        ClientRegistration client_registration = 1;
        repeated DeviceRegistration device_registrations = 2;
        Subscribtion subscribtion = 3;
    }
}