package ubii;
syntax = "proto3";

import "./request/clientRegistration.proto";
import "./request/deviceRegistration.proto";
import "./request/subscribtion.proto";

message ServiceRequest {
    oneof type{
        ClientRegistration client_registration = 0;
        repeated DeviceRegistration device_registrations = 1;
        Subscribtion subscribtion = 2;
    }
}