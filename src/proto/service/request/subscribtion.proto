package ubii.subscribtion;
syntax = "proto3";

import "./topicRecord.proto";

message Subscribtion {
    string device_id = 1;
    repeated string subscribtion_topics = 2;
    repeated string unsubscribtion_topics = 3;
}