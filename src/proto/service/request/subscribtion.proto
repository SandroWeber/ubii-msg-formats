package ubii.subscribtion;
syntax = "proto3";

import "./topicRecord.proto";

message Subscribtion {
    string device_id = 0;
    repeated string subscribe_topics = 1;
    repeated string unsubscribe_topics = 2;
}