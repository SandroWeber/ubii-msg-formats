[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "importlib_metadata;python_version<='3.7'",
    "ubii-msg-compiler[protoplus] >= 0.0.4a1dev1",
    "setuptools_scm>=6.2",
]

build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
local_scheme = "no-local-version"
