[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "setuptools_scm>=6.2",
    "ubii-msg-compiler[mypy] @ git+https://github.com/saggitar/ubii-msg-compiler.git@develop",
]

build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
