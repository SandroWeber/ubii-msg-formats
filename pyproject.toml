[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "ubii-msg-compiler @ git+https://github.com/saggitar/ubii-msg-compiler.git@v0.0.2-alpha",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
    "test/pytest",
]

