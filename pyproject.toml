[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "ubii-msg-compiler[protoplus] >= 0.0.3rc1.dev4",
    "setuptools_scm>=6.2",
]

build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
local_scheme = "no-local-version"
